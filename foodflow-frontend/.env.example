# Points the frontend at your backend API. For local development, leave this
# as-is (or omit the file entirely - it falls back to localhost automatically).
# When deployed, set this to your real backend's URL (e.g. https://your-app.onrender.com)
VITE_API_URL=http://localhost:8080
